"""ITles reference stand: protocol-exact models of machines, trackers and ingestion.

The stand never fakes the software under test: packets are built byte-for-byte
per public protocol specifications and decoded by the real gateway (Traccar).
Only the physical machine and the radio environment are modelled.
"""
